<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <!-- todo Editext for Search -->
    <android.support.design.widget.TextInputLayout
        android:id="@+id/input_layout_searchExercise"
        android:layout_width="match_parent"
        android:layout_marginLeft="@dimen/search_exercise_margin"
        android:layout_marginTop="@dimen/search_exercise_margin"
        android:layout_marginRight="@dimen/search_exercise_margin"
        android:layout_height="wrap_content"
        android:visibility="gone">

        <EditText
            android:id="@+id/et_searchExercise"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/search_exercise" />

    </android.support.design.widget.TextInputLayout>

    <!-- List View -->
    <ListView
        android:layout_below="@+id/input_layout_searchExercise"
        android:id="@+id/lv_exercises"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_exercises"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        android:src="@drawable/ic_add_white_36dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true" />

</RelativeLayout>
